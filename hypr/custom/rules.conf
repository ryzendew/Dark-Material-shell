# You can put custom rules here
# Window/layer rules: https://wiki.hyprland.org/Configuring/Window-Rules/
# Workspace rules: https://wiki.hyprland.org/Configuring/Workspace-Rules/

## applicationMenu:blur - Quickshell layer
layerrule = blur, ^(quickshell:applicationMenu:blur)$
layerrule = ignorealpha 0.6, ^(quickshell:applicationMenu:blur)$
layerrule = ignorezero, ^(quickshell:applicationMenu:blur)$
layerrule = xray 0a, ^(quickshell:applicationMenu:blur)$

# Legacy applicationMenu:blur rules (keeping for compatibility)
layerrule = blur,^(quickshell:bar:blur)$
layerrule = ignorezero,^(quickshell:bar:blur)$
layerrule = blur,^(quickshell:dock:blur)$
layerrule = xray 1, ^(quickshell:dock:blur)$ 

windowrulev2 = rounding 30,class:^(quickshell)$
windowrulev2 = nofocus,class:^(quickshell)$

# Layer rules for proper blur handling
layerrule = ignorezero,quickshell:dock:blur 

# Sidebar rules
layerrule = blur,^(quickshell:sidebarLeft)$
layerrule = ignorezero,^(quickshell:sidebarLeft)$
windowrulev2 = rounding 30,class:^(quickshell:sidebarLeft)$

layerrule = blur,^(quickshell:sidebarRight)$
layerrule = ignorezero,^(quickshell:sidebarRight)$
windowrulev2 = rounding 30,class:^(quickshell:sidebarRight)$
layerrule = xray 0, ^(quickshell:sidebarRight:blur)$ 

# Cider

layerrule = blur,cider
layerrule = xray,cider
layerrule = ignorezero,cider

## Hyprswitch

layerrule = blur,hyprswitch
layerrule = xray,hyprswitch
layerrule = ignorezero,hyprswitch

## Weather widget
layerrule = blur, ^(quickshell:weather)$
layerrule = ignorezero,^(quickshell:weather)$

## File manager 

layerrule = blur,nautilus
layerrule = xray,nautilus
layerrule = ignorezero,nautilus

# Hyprland rules for quickshell control panel
layerrule = blur, ^(quickshell:controlpanel:blur)$
layerrule = ignorealpha 0.6, ^(quickshell:controlpanel:blur)$
layerrule = ignorezero, ^(quickshell:controlpanel:blur)$
layerrule = xray 1, ^(quickshell:controlpanel:blur)$ 

# OSD (On-Screen Display) blur rules
layerrule = blur, ^(quickshell:onScreenDisplay)$
layerrule = ignorealpha 0.3, ^(quickshell:onScreenDisplay)$
layerrule = ignorezero, ^(quickshell:onScreenDisplay)$

# Microphone OSD blur rules
layerrule = blur, ^(quickshell:onScreenDisplayMicrophone)$
layerrule = ignorealpha 0.3, ^(quickshell:onScreenDisplayMicrophone)$
layerrule = ignorezero, ^(quickshell:onScreenDisplayMicrophone)$

# Session screen blur rules
layerrule = blur, ^(quickshell:session)$
layerrule = ignorealpha 0.3, ^(quickshell:session)$
layerrule = ignorezero, ^(quickshell:session)$

# Reload popup blur rules (apply to all quickshell popup windows)
windowrulev2 = opacity 0.95, class:^(quickshell)$

# QuickShell Cheatsheet - Transparent with blur
layerrule = blur, ^(quickshell:cheatsheet)$
layerrule = ignorealpha 0.3, ^(quickshell:cheatsheet)$
layerrule = ignorezero, ^(quickshell:cheatsheet)$

layerrule = blur, ^(quickshell:simpleMediaPlayer)$
layerrule = ignorealpha 0.3, ^(quickshell:simpleMediaPlayer)$
layerrule = ignorezero, ^(quickshell:simpleMediaPlayer)$

# Settings window blur rules
layerrule = blur, ^(quickshell:settings:blur)$
layerrule = ignorealpha 0.6, ^(quickshell:settings:blur)$
layerrule = ignorezero, ^(quickshell:settings:blur)$
layerrule = xray 0, ^(quickshell:settings:blur)$

# App drawer popout blur rules
layerrule = blur, ^(quickshell:appDrawerPopout:blur)$
layerrule = ignorealpha 0.6, ^(quickshell:appDrawerPopout:blur)$
layerrule = ignorezero, ^(quickshell:appDrawerPopout:blur)$
layerrule = xray 0, ^(quickshell:appDrawerPopout:blur)$

# Notification popup blur rules
layerrule = blur, ^(quickshell:notification:blur)$
layerrule = ignorealpha 0.6, ^(quickshell:notification:blur)$
layerrule = ignorezero, ^(quickshell:notification:blur)$
layerrule = xray 0, ^(quickshell:notification:blur)$

# Modal blur rules (settings, etc.)
layerrule = blur, ^(quickshell:modal)$
layerrule = ignorealpha 0.6, ^(quickshell:modal)$
layerrule = ignorezero, ^(quickshell:modal)$
layerrule = xray 0, ^(quickshell:modal)$

# Popout blur rules (DankDash, ControlCenter, etc.)
layerrule = blur, ^(quickshell:popout)$
layerrule = ignorealpha 0.6, ^(quickshell:popout)$
layerrule = ignorezero, ^(quickshell:popout)$
layerrule = xray 0, ^(quickshell:popout)$

# Control Center specific blur rules
layerrule = blur, quickshell:controlCenter:blur
layerrule = ignorealpha 0.6, quickshell:controlCenter:blur
layerrule = ignorezero, quickshell:controlCenter:blur
layerrule = xray 0, quickshell:controlCenter:blur

# Dock context menu blur rules
layerrule = blur, ^(quickshell:dock:contextMenu:blur)$
layerrule = ignorealpha 0.6, ^(quickshell:dock:contextMenu:blur)$
layerrule = ignorezero, ^(quickshell:dock:contextMenu:blur)$
layerrule = xray 0, ^(quickshell:dock:contextMenu:blur)$

windowrulev2 = rounding 30, class:^(quickshell)$

# Desktop Widget Blur Rules
# Enable blur for desktop widgets (CPU temp, GPU temp, System Monitor, Clock)
layerrule = blur, ^(quickshell:desktop:widget)$
layerrule = ignorealpha 0.6, ^(quickshell:desktop:widget)$
layerrule = ignorezero, ^(quickshell:desktop:widget)$
layerrule = xray 0, ^(quickshell:desktop:widget)$

# Specific desktop widget blur rules
layerrule = blur, ^(quickshell:desktop:cpuTemp)$
layerrule = ignorealpha 0.6, ^(quickshell:desktop:cpuTemp)$
layerrule = ignorezero, ^(quickshell:desktop:cpuTemp)$
layerrule = xray 0, ^(quickshell:desktop:cpuTemp)$

layerrule = blur, ^(quickshell:desktop:gpuTemp)$
layerrule = ignorealpha 0.6, ^(quickshell:desktop:gpuTemp)$
layerrule = ignorezero, ^(quickshell:desktop:gpuTemp)$
layerrule = xray 0, ^(quickshell:desktop:gpuTemp)$

layerrule = blur, ^(quickshell:desktop:systemMonitor)$
layerrule = ignorealpha 0.6, ^(quickshell:desktop:systemMonitor)$
layerrule = ignorezero, ^(quickshell:desktop:systemMonitor)$
layerrule = xray 0, ^(quickshell:desktop:systemMonitor)$

layerrule = blur, ^(quickshell:desktop:clock)$
layerrule = ignorealpha 0.6, ^(quickshell:desktop:clock)$
layerrule = ignorezero, ^(quickshell:desktop:clock)$
layerrule = xray 0, ^(quickshell:desktop:clock)$

layerrule = blur, ^(quickshell:desktop:weather)$
layerrule = ignorealpha 0.6, ^(quickshell:desktop:weather)$
layerrule = ignorezero, ^(quickshell:desktop:weather)$
layerrule = xray 0, ^(quickshell:desktop:weather)$

# Desktop widget window rules
windowrulev2 = nofocus, class:^(quickshell)$
windowrulev2 = stayfocused, class:^(quickshell)$
windowrulev2 = noanim, class:^(quickshell)$
windowrulev2 = noshadow, class:^(quickshell)$
windowrulev2 = float, class:^(quickshell)$
